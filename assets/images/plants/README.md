Bundled plant images live in this folder.

Rules:
- Only add manually reviewed public-domain or CC0 assets here.
- Keep the original source metadata in `src/data/imageManifest.ts`.
- Add the matching `assetKey` entry in `src/data/imageAssets.ts` after the file is downloaded.
- Do not point the live app at third-party runtime image URLs.
